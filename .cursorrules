Мы создаем аналоги популярной игры Jeopardy.

Пользователь может быть как в роли игрока, так и в роли ведущего.
Ведущий всегда один. Игроков может быть не меньше 2.
Ведущий заранее создает вопросы и ответы.

Когда пишешь код, будь 100% уверен, что он будет работать.

Использовать нужно только последний Angular 19:
- Для входных параметров: использовать `input()` вместо `@Input()`
- Для выходных событий: использовать `output()` вместо `new EventEmitter<T>()`
- В шаблонах: использовать `@for`, `@if`, `@switch`, `@let` вместо `*ngFor`, `*ngIf` и т.д.
- Для состояния: использовать сигналы (`signal()`, `computed()`) вместо обычных свойств
- Для побочных эффектов: использовать `effect()`

НИКОГДА НЕ ИСПОЛЬЗУЙ:
- Декораторы `@Input`, `@Output`, `@ViewChild`, `@ViewChildren`, `@ContentChild`, `@ContentChildren`
- Директивы `*ngIf`, `*ngFor` и другие структурные директивы со звездочкой

Перед методами в ts файлах всегда добавляй модификаторы `private` или `public`.

Все тексты должны быть на английском языке. Если где-то встречается текст на русском, переведи его на английский.

Основной стек технологий:
- Angular 19
- Tailwind CSS
- TypeScript
- RxJS
- NgRx
- Spartan